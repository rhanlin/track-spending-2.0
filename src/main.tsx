import ViteReact from 'react'
import ReactDOM from 'react-dom'

import App from './App'

ReactDOM.render(
  <ViteReact.StrictMode>
    <App />
  </ViteReact.StrictMode>,
  document.getElementById('root')
)
